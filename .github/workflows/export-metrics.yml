name: Export Snowflake Marketplace Metrics

on:
  schedule:
    - cron: '0 6 * * *'  # Daily at 06:00 UTC
  workflow_dispatch:
    inputs:
      listing_filter:
        description: 'Listing name filter (SQL LIKE pattern)'
        required: false
        default: '%FALKORDB%'
      days:
        description: 'Lookback window in days'
        required: false
        default: '90'

jobs:
  export-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: metrics/requirements.txt

      - name: Install dependencies
        run: pip install -r metrics/requirements.txt -c metrics/constraints.txt

      - name: Export metrics from Snowflake
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
        run: |
          python metrics/export_metrics.py \
            --listing-filter "${{ inputs.listing_filter || '%FALKORDB%' }}" \
            --days "${{ inputs.days || '90' }}" \
            --output metrics_report.json

      - name: Show summary
        run: |
          echo "### Snowflake Marketplace Metrics" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          python -c "
          import json
          with open('metrics_report.json') as f:
              data = json.load(f)
          print(json.dumps(data['summary'], indent=2))
          " >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Upload metrics report
        uses: actions/upload-artifact@v4
        with:
          name: sf-metrics-${{ github.run_id }}
          path: metrics_report.json
          retention-days: 90
